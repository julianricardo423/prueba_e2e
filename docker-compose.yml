services:
  tests:
    environment:
      USER_BROWSERSTACK: ${USER_BROWSERSTACK}
      KEY_BROWSERSTACK: ${KEY_BROWSERSTACK}
    image: maven:3.8.8
    platform: linux/amd64
    working_dir: /src
    volumes:
      - /var/jenkins_home/workspace/my-pipeline:/src