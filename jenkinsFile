pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        withMaven(maven: 'maven-3.8.8') {
            sh 'mvn --version'
            sh 'mvn clean verify'
        }
      }
    }
  }
}